You may need to download selenium and webdrive-manager

Do so in this directory with the following commands: 
pip install selenium
pip install webdrive-manager

or 

pip3 install selenium
pip3 install webdrive-manager